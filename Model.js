import {
    AsyncStorage
} from "react-native";

export default class Model {
    constructor(serializableProperties) {
        this._serializableProperties = serializableProperties;
    }

    serialize() {
    }

    deserialize() {
    }
}
